{% extends 'base.html.twig' %}

{% block title %}ProduitImage{% endblock %}

{% block body %}
    <h1>ProduitImage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit_image.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ produit_image.nom }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ produit_image.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_produit_image_index') }}">back to list</a>

    <a href="{{ path('app_produit_image_edit', {'id': produit_image.id}) }}">edit</a>

    {{ include('produit_image/_delete_form.html.twig') }}
{% endblock %}
